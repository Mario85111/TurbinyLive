# dependencies
node_modules
functions/node_modules
.pnp
.pnp.js

# build
dist
dist-ssr

# env (klucz API nie trafia do repo!)
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / os
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.local

# firebase
.firebase
firebase-debug.log*

# cache narzedzi analizujacych kod (generowany, nie zrodlo)
graphify-out/

# lokalne ustawienia Claude Code (per-maszyna); .claude/launch.json zostaje w repo
.claude/settings.local.json
